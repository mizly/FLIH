# Set a long random secret to allow hardware updates. Never expose it to the browser.
ROBOT_API_KEY=

# Huawei OMNI / Yibu API (server-side only; never expose this to the browser).
YIBU_API_KEY=
YIBU_BASE_URL=https://yibuapi.com/v1
YIBU_MODEL=qwen3.5-omni-plus